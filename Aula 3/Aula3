Dependendo da arquitetura de onde está sendo executado o programa, o int pode ter tamanhos diferentes. Não da para garantir o quanto cabe no Int.
Por isso, é importante teste a variável com o operador sizeof, e as constantes UINT_MAX, INT_MAX

Existe uma forma de dizer quantos bytes vc quer em uma variável, usando  do <stdint.h>